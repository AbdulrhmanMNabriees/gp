$(function () {

    // Upload files
    $('input:file.upload-file').fileuploader({

    });

    // Enabel Tooltips every where
    $(function () {
        $('[data-toggle="tooltip"]').tooltip()
    })
});